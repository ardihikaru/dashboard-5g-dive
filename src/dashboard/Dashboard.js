import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

const Dashboard = () => {
    return (
        <Card>
            <CardHeader title="Welcome to the EagleEYE Dashboard" />
            <CardContent>This is the front-end application for the final project of Mobile Design cuorse.</CardContent>
        </Card>
    );

};

export default Dashboard;
